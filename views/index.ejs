<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    
    <div class="main w-full min-h-screen bg-zinc-800">
        <div class="form px-2 py-3 text-white">
            <form action="/create" method="post">
                <input class="block px-3 py-4 rounded-lg bg-zinc-700 w-full " type="text" placeholder="Title" name="title">
                <textarea class="block px-3 py-4 rounded-lg bg-zinc-700 resize-none w-full mt-2" placeholder="write your task here" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-4 py-2 rounded-lg" type="submit" value= "Create task">
            </form>
        </div>

        <div class="tasks px-2 py-3 flex gap-3 flex-wrap">

            <% if(files.length > 0){ %>

                <% files.forEach(function(val){ %>
                    <div class="task min-w-72 bg-zinc-700 px-3 py-4 rounded-lg text-white">
                        <h2 class="text-3xl tracking-tighter"><%= val %></h2>
                        <a class="text-blue-500 inline-block mt-2" href="/file/<%= val %>">Read More</a>
                    </div>
                    <%  }) %>

              <% } else{ %>
                <h3 class="text-zinc-500">No Tasks yet</h3>
            <% } %>



        </div>
    </div>
    
    <script src="javascripts/script.js"></script>
</body>
</html> 